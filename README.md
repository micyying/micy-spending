# Micy花在哪

把不同銀行的信用卡月結單交易集中整理成統一帳本，支援 PDF 匯入、交易搜尋、卡片／月份／分類篩選、備份匯出，以及可選的端對端加密同步。

這是獨立於「Micy刷哪張」的 GitHub Pages 專案，使用自己的本機儲存鍵、備份格式及同步命名空間，不會讀取或覆蓋另一個 app 的資料。

## 私隱

- 交易與原始 PDF 預設只保存在目前裝置的瀏覽器。
- 如啟用同步，交易資料會在裝置上加密後才上傳；同步代碼包含解密所需資訊，請妥善保存。
- GitHub repository 只包含 app 程式碼，不包含使用者上傳的月結單或交易資料。
